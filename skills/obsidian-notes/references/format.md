# Format

- Start with YAML frontmatter when the user wants tags or aliases.
- Use `[[Note Name]]` for vault links.
- Use `> [!note]` callouts only when they add structure.
- Keep one H1 that matches the note title.
